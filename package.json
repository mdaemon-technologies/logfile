{
  "name": "@mdaemon/logfile",
  "version": "3.8.0",
  "description": "A node logging utility",
  "type": "module",
  "main": "dist/logfile.cjs",
  "module": "dist/logfile.mjs",
  "typings": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "test": "npm run test:source && npm run build:bundle && npm run test:types && npm run test:cjs",
    "test:source": "cross-env TEST_TARGET=source jest --config=jest.config.cjs",
    "test:cjs": "cross-env TEST_TARGET=compiled-cjs jest --config=jest.config.cjs",
    "test:min": "npm run build && npm run test:cjs",
    "test:all": "npm run test && npm run test:min",
    "dev-test": "cross-env TEST_TARGET=source jest --watchAll --config=jest.config.cjs",
    "build:bundle": "rollup -c && node scripts/prune-types.mjs",
    "build:minify": "esbuild dist/logfile.cjs --minify --outfile=dist/logfile.cjs --allow-overwrite && esbuild dist/logfile.mjs --minify --outfile=dist/logfile.mjs --allow-overwrite",
    "build": "npm run build:bundle && npm run build:minify",
    "dev": "rollup -c -w",
    "prepublishOnly": "npm run build",
    "pub": "npm run test:all && npm publish",
    "test:types": "tsc -p tsconfig.types.json && node scripts/prune-types.mjs --check"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mdaemon-technologies/logfile.git"
  },
  "keywords": [
    "logging",
    "logfile",
    "node",
    "log to file",
    "log",
    "debug log",
    "fs"
  ],
  "author": {
    "name": "MDaemon Technologies, Ltd.",
    "email": "jonathan.ehman@mdaemon.com",
    "url": "https://www.mdaemon.com"
  },
  "license": "LGPL-2.1",
  "bugs": {
    "url": "https://github.com/mdaemon-technologies/logfile/issues"
  },
  "homepage": "https://github.com/mdaemon-technologies/logfile#readme",
  "devDependencies": {
    "@rollup/plugin-typescript": "^12.1.2",
    "@types/jest": "^30.0.0",
    "cross-env": "^7.0.3",
    "esbuild": "^0.25.0",
    "jest": "^30.0.0",
    "rollup": "^4.40.0",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2",
    "tslib": "^2.8.1",
    "typescript": "^6.0.0"
  },
  "files": [
    "dist",
    "CHANGELOG.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/logfile.mjs",
      "require": "./dist/logfile.cjs"
    }
  }
}
